[build]
  command = "npm run build"
  publish = ".next"

# Netlify's Next.js runtime is detected automatically. Environment variables are
# set in the Netlify UI (Site configuration -> Environment variables).
